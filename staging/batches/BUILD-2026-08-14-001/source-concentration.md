# Source concentration

The framework release contains zero production sources, so no research-corpus publisher share can be calculated. The six accepted architecture sources are all OFF-owned repositories because the master build prompt explicitly required OFF convention inspection; they are not represented as independent evidence or as a production research corpus.
