# Migrations

Schema and data migrations are explicit, versioned, testable, and reversible. Each migration records source and target schema versions, affected entity types, deterministic transformation, validation before and after, rollback instructions, retired IDs, and release linkage. Research batches do not modify schemas unless explicitly authorized.
